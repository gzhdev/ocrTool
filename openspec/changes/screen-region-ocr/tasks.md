# 任务：屏幕区域截图识别

## 0. 前置确认

- [x] 0.1 确认 `mvp-image-ocr` 已完成，图片 → 预览 → 识别 → 结果的管线可正常工作
  - 全量 195 测试通过（`uv run pytest tests/ -q`）
- [x] 0.2 确认当前开发环境的显示器数量与各自缩放比例，记录下来；若为单显示器 100%，安排任务 1.6 的验证环境
  - 本机单显示器 `\\.\DISPLAY2`，逻辑 1920×1080，DPR 1.0（100%），主屏。无第二块物理显示器；
    1.6 按设计书风险表预案执行：QT_SCALE_FACTOR=1.25 制造非 100% 缩放实测 + 合成混合 DPI（1.0/1.5 双屏）单测

## 1. 捕获与坐标（风险集中区，先于其余任务完成）

- [x] 1.1 枚举所有屏幕，记录每块屏幕的逻辑几何与缩放比例，输出到日志
  - `capture/screen_capture.py::log_screen_layout`（logger `ocrtool.capture`，只记几何与比例）
- [x] 1.2 实现逐屏捕获，得到每块屏幕的物理像素图像
  - `capture_screen()`：`QScreen.grabWindow(0)` 逐屏调用；空图像 → `ScreenCaptureError`（远程桌面等环境可读报错）
- [x] 1.3 实现逐屏的「逻辑选区 → 物理像素矩形」换算，使用该屏自身的缩放比例
  - `logical_to_physical()`：全局逻辑 → 屏内局部 → ×该屏 DPR → toAlignedRect → 夹取捕获图范围
- [x] 1.4 用临时入口验证：在屏幕上框选一个内容已知的区域（如一段特定文字），保存出的图像内容与框选范围逐像素对应
  - 临时入口 `scripts/dev_verify_capture.py`：满屏坐标编码色块（32 逻辑像素/块）→ 逐屏捕获 → 逐点映射采样对照 + 裁剪路径核对。
    本机 DPR 1.0：**98/98 逐像素对照通过**，裁剪尺寸与块色吻合
- [x] 1.5 验证选区覆盖层能显示在常见置顶窗口之上
  - 阶段 B：创建 `WindowStaysOnTopHint` 辅助窗后展示覆盖层，`EnumWindows` Z 序实测覆盖层(18) 在置顶辅助窗(19) 之上
- [x] 1.6 **决策关卡**：在真实多显示器 + 不同缩放比例（至少一块非 100%）环境下重复 1.4，每块屏幕都正确。若坐标无法稳定正确，停止本变更并重新评估捕获方案
  - 无多显示器硬件，按预案以缩放差异替代：QT_SCALE_FACTOR=1.25（逻辑 1536×864 ↔ 物理 1920×1080）实测 **66/66 通过**；
    混合 DPI 由合成双屏（DPR 1.0 / 1.5）单测覆盖（每屏用自身比例独立换算、互不串扰）。**关卡通过**
- [x] 1.7 删除临时入口
  - `scripts/dev_verify_capture.py` 已删除

## 2. 主窗口隐藏与恢复

- [x] 2.1 实现发起截图时隐藏主窗口，并等待隐藏实际生效后再捕获
  - `RegionCaptureFlow._hide_and_wait`：窗口隐藏 + 事件循环确认不再暴露 + 200ms 下限延时双条件（design D4，
    不以固定睡眠为唯一手段）；单测断言捕获时刻窗口不可见、未暴露、已等待≥下限
- [x] 2.2 验证：主窗口覆盖在目标内容上时发起截图，捕获画面中不含主窗口
  - 真实桌面临时脚本：真 MainWindow 覆盖在满屏坐标编码色块上发起流程，窗口原区域采样 **54/54 全为色块**
- [x] 2.3 在低性能条件下重复 2.2（可通过施加 CPU 负载模拟），确认不出现窗口残影
  - 每逻辑核一个忙循环进程满载后复测：**54/54 全为色块**，无残影；脚本验收后删除
- [x] 2.4 实现流程结束后恢复主窗口到发起前的可见状态
  - `_cleanup` 记录发起前可见性：可见则 show + raise + activate；发起前不可见则保持不可见（单测覆盖两分支）

## 3. 选区交互与取消

- [x] 3.1 实现按下 - 拖动 - 释放的选区交互，选区外区域弱化显示
  - `RegionOverlay`：按下锚点/拖动实时更新/释放取权威终点；含端点矩形显式构造（规避 QRect 两点构造在反向拖动的取整不确定）；
    绘制冻结帧 + 选区外 110 透明度弱化 + 边框与物理像素尺寸提示
- [x] 3.2 实现选区在屏幕边界处截止，不延伸到相邻屏幕
  - 指针位置夹取在本屏 `(width-1, height-1)` 内；单测：拖出 800×600 屏后选区止于 799/599
- [x] 3.3 实现 ESC 与右键取消
  - keyPressEvent(Escape) / 右键 press 即取消（拖动中也生效）；单测覆盖
- [x] 3.4 实现面积过小的选区按取消处理，验证单击不会产生识别请求也不弹出错误
  - 任一边 < 8 物理像素按取消；单击/过小拖动单测：只发 cancelled，无 selectionDone、无错误
- [x] 3.5 实现所有屏幕覆盖层的一致关闭，验证在任一屏幕完成或取消后其余屏幕的覆盖层同时消失
  - 结束路径汇聚 `_cleanup` 统一销毁全部覆盖层；双屏合成单测：任一屏取消后两覆盖层同时不可见
- [x] 3.6 实现单一收尾出口，在捕获或选区过程中主动注入异常，验证覆盖层被销毁且主窗口恢复
  - `start` 全程 try/except → `_abort`；`_on_selection_done` 裁剪异常同样注入。单测两条异常路径：捕获抛错与
    越界选区裁剪失败，均断言覆盖层销毁 + 主窗口恢复 + failed 携带可读信息
- [x] 3.7 验证取消后立即再次发起截图可正常工作
  - 流程对象一次性 + `_started` 重入守卫；单测：取消后新建流程立即发起，隐藏/选区/恢复全部正常

## 4. 接入识别管线

- [x] 4.1 将捕获结果作为第四种输入接入，复用既有的载入与预览路径
  - `MainWindow.start_region_capture / _on_capture_finished`：与粘贴同路——`qimage_to_bgr` + `scale_to_limit`
    + `_set_current_image`（预览原图 + 缩放识别副本），不新增识别路径与结果结构
- [x] 4.2 确认捕获图像跳过扩展名与可解码校验
  - 捕获已是内存 QImage，直接进转换；单测 monkeypatch `validate_and_load` 为必须抛错，路径仍正常
- [x] 4.3 验证超大选区按既有等比缩放规则处理，结果携带正确的缩放比例
  - 单测：7000×400 捕获 → 预览原图 7000 宽，识别副本 ≤6000，`_pending_scale < 1.0`
- [x] 4.4 实现完成选区后自动发起识别
  - `_on_capture_finished` 末尾直调 `start_recognition`（design D7，唯一自动触发识别的输入）；单测断言结果面板已出文本
- [x] 4.5 验证识别期间截图入口按既有规则禁用
  - `_update_actions` 将 `_capture_action` 与打开/粘贴并列 busy 禁用；单测与既有入口行为一致
- [x] 4.6 验证截图识别过程中不产生任何磁盘图像文件或临时文件
  - 单测跑完整流程（选区→裁剪→识别）后扫描 USER_ROOT：无 .png/.jpg/.jpeg/.bmp/.webp/.tmp；
    capture 模块 grep 确认无 `.save(`/`tempfile`/`QTemporaryFile` 出口

## 5. 自动复制

- [x] 5.1 新增 `ui.auto_copy` 配置项，默认开启，纳入三层合并
  - `defaults.py` 与 `config/default.json` 同步加 `"ui": {"auto_copy": true}`（字段集合一致性测试守护）；
    用户层覆盖关闭有专门单测
- [x] 5.2 实现识别成功且检出文本时自动写入剪贴板，状态区给出提示
  - `_on_result_ready`：`result.text` 非空白且配置开启 → `set_clipboard_text` + 状态区「识别结果已自动复制到剪贴板」
- [x] 5.3 验证空结果时剪贴板内容不变：先复制一段已知文本，再截取一块无文字区域，确认原文本仍在
  - 单测：预置「用户既有的重要文本」→ 空结果识别 → 断言剪贴板原样（design D5 数据破坏防护）
- [x] 5.4 验证识别失败时剪贴板内容不变，方法同 5.3
  - 单测：识别抛 RecognitionError → 剪贴板原样（resultReady 不发射即不写）
- [x] 5.5 验证关闭自动复制后手动复制仍然可用
  - 单测：`ui.auto_copy=false`（DottedDict 点路径替身）→ 成功结果不改剪贴板；`copy_all()` 手动复制仍写入

## 6. 收尾

- [x] 6.1 补全设计书 §3.4：多显示器、DPI、冻结帧、取消语义、选区不跨屏的设计说明
  - §3.4 重写为流程图 + 8 条设计要点（冻结帧 / 逐屏捕获与坐标换算 / 不跨屏 / 隐藏双条件 / 取消语义 /
    单一收尾出口 / 第四输入 / 快捷键与降级）；§9.1 同步落地 `ui.auto_copy: true` 并标注旧草案键未实现
- [x] 6.2 在设计书 §36 勾选「截图 OCR」与「自动复制」
- [x] 6.3 确认日志中不含识别文本，截图路径未破坏 `app-logging` 的隐私约定
  - capture 模块日志仅含屏幕名/几何/缩放/物理尺寸/耗时与异常（grep 核查无文本写出口）；
    既有隐私集成测试（识别文本不落日志/stdout）在全量套件中通过
- [x] 6.4 `openspec validate --strict screen-region-ocr` 通过
  - 校验输出 valid；全量测试 239 通过（195 既有 + 44 新增）
