# 任务：后台常驻与系统集成

严格按任务组顺序实施：单实例 → 托盘 → 快捷键 → 开机启动。每组验证通过再进入下一组。

## 0. 前置确认

- [x] 0.1 确认 `mvp-image-ocr` 已完成
  - 实施注记：已归档于 `archive/2026-08-22-mvp-image-ocr/`（tasks 32/32）
- [x] 0.2 确认 `screen-region-ocr` 的状态。若已完成，任务 5.1 将快捷键绑定到截图识别；若未完成，快捷键先绑定「显示主窗口」，不阻塞本变更
  - 实施注记：已完成并归档（`archive/2026-08-24-screen-region-ocr/`）→ 任务 5.1 快捷键绑定截图识别

## 1. 单实例（其余功能的前提）

- [x] 1.1 实现实例端点，名称由当前用户与程序所在目录共同派生
  - 实施注记：`platform/single_instance.py::endpoint_name` = sha256(用户|目录小写归一)[:16]；路径参与哈希前 casefold（Windows 命名管道不区分大小写）。Qt 映射：QLocalServer 在 Windows 即命名管道
- [x] 1.2 实现启动检测：先尝试连接已有端点，连接成功即视为已有实例
  - 实施注记：`SingleInstanceGuard.check_and_listen` 先 QLocalSocket 探测连接（1500ms 超时），成功即 ALREADY_RUNNING 并写入激活意图后退出本次启动
- [x] 1.3 实现陈旧端点清理：连接不上时先移除同名遗留端点再建立监听
  - 实施注记：连不上 → `QLocalServer.removeServer(name)` 显式移除遗留端点 → `listen`。Windows 命名管道随进程终止由系统回收（removeServer 为无害空操作，调用保持跨平台语义）；Unix 套接字文件残留由此调用真正清除
- [x] 1.4 **验证陈旧端点处理**：用任务管理器强制结束进程，随后重新启动，确认能正常启动；连续重复 5 次，每次都能启动
  - 实施注记：`tests/integration/test_single_instance_integration.py::test_强杀后连续五次重启均能正常启动`——taskkill /F（无任何清理路径）× 5 轮全部 first_instance，passed
- [x] 1.5 实现重复启动时激活已有实例的窗口
  - 实施注记：guard 的 `activationRequested` 在 application 接线到 `window.bring_to_front()`；集成测试已验证真实进程间激活消息送达（`test_重复启动请求激活且自身不驻留`）
- [x] 1.6 验证已有实例分别处于「窗口可见」「最小化」「隐藏」三种状态时，重复启动均能正确置于前台
  - 实施注记：`test_tray_ui.py::TestBringToFront` 三状态参数化（bring_to_front 共用于托盘/快捷键/二次启动）；最小化还原保留最大化标志
- [x] 1.7 验证两个不同目录下的副本可同时运行
  - 实施注记：端点按目录派生（单测）+ 真实双进程驻留互不干扰（集成 `test_不同端点名的两个实例可同时驻留`）
- [x] 1.8 实现检测超时与机制不可用时的降级：仍然启动并记录日志
  - 实施注记：连接超时（ConnectingState 停留）按无实例继续 + WARNING；listen 失败 / 检测环节异常 → DEGRADED + ERROR 日志，均不阻塞启动（单测 3 例覆盖）
- [x] 1.9 验证退出时实例端点被释放
  - 实施注记：`shutdown()` 关闭 server 与已接受连接；真实进程正常退出后新实例同名监听成功（集成 `test_正常退出后端点可被新实例复用`）

## 2. 系统托盘

- [x] 2.1 准备多尺寸托盘图标资源，纳入发布产物；在高 DPI 显示器上确认清晰
  - 实施注记：`resources/icons/tray-{16,24,32,48,256}.png`（256 基准 LANCZOS 缩放，蓝底白「文」），生成脚本 `scripts/gen_tray_icon.py`，产物入库；QIcon 多尺寸注册后按实际显示尺寸自动取位图；`build.ps1` 步骤 6 已补 resources 复制
- [x] 2.2 实现托盘图标与菜单（显示主窗口、退出）
  - 实施注记：`ui/tray.py::TrayController`——菜单含 显示主窗口/截图识别/开机启动/关闭时驻留托盘/退出；单击与双击图标均请求显示主窗口；程序化刷新勾选 blockSignals 防回灌
- [x] 2.3 新增 `ui.close_to_tray` 配置项并实现两种关闭行为
  - 实施注记：默认 False（design Open Question：默认退出、常驻由用户主动开启）；托盘菜单勾选项 + `MainWindow.set_close_to_tray` 持久化；closeEvent 分支见 test_tray_ui.py
- [x] 2.4 **解除界面框架「无可见窗口即退出」的默认行为**，同批实现明确的退出入口
  - 实施注记：`run()` 中 `app.setQuitOnLastWindowClosed(False)`；退出入口两处——托盘菜单「退出」、关闭窗口且不驻留（`quitRequested` 信号）；两者同批落地（design D7：解除与退出入口是一组）
- [x] 2.5 验证：主窗口隐藏到托盘后进程仍存活，且可通过托盘菜单退出；确认不会产生只能用任务管理器结束的进程
  - 实施注记：隐藏分支不 emit quit（单测 `test_驻留开启且托盘可用_关闭为隐藏`）；托盘退出 = quitRequested→app.quit（信号连通性单测）；真实进程驻留与正常退出走 shutdown 释放（集成 `test_正常退出后端点可被新实例复用`）——不存在无退出路径的状态
- [x] 2.6 实现首次隐藏到托盘时的一次性提示，验证此后不再重复出现
  - 实施注记：`ui.tray_hint_done` 配置标记；`test_首次驻留提示只出现一次`（再次关闭不再提示且已持久化）
- [x] 2.7 实现退出时释放全部系统资源（快捷键、实例端点、托盘图标）
  - 实施注记：`run()` 的 aboutToQuit → `_release_resources` 显式调用 hotkey.shutdown() + guard.shutdown() + tray.shutdown()，并在控制器私有容量 1 线程池上有界排空（review 75-3 修正）
- [x] 2.8 验证识别进行中请求退出可正常退出
  - 实施注记：closeEvent 不因 busy 拒绝（单测 `test_识别进行中关闭不阻止退出`）；后台识别收尾由 aboutToQuit 的 waitForDone(10s) 承担，超时强退避免杀不掉
- [x] 2.9 实现托盘不可用时降级为前台程序，且此时关闭窗口即退出
  - 实施注记：`TrayController.available=False` 时不创建托盘对象 + WARNING 日志；`close_to_tray` 分支要求 tray_available——托盘不可用时关闭即退出（单测 `test_驻留开启但托盘不可用_仍退出`）

## 3. 全局快捷键

- [x] 3.1 实现注册式热键与消息接收，确认长按不重复触发
  - 实施注记：`platform/hotkey.py`——ctypes 直调 RegisterHotKey/UnregisterHotKey（零第三方依赖，非键盘监听，design D5），WM_HOTKEY 经 QAbstractNativeEventFilter 接收。长按去重为「等待组合释放」状态机 + 30ms 键态轮询（GetAsyncKeyState）：纯时间阈值在 Windows 约 500ms 键盘重复延迟后会放行重复投递（真实桌面实测发现）。端到端实测：隐藏窗口触发 1 次、持续按住 + 6 连重复恰 1 次、未注册组合 0 次（SendInput 真实合成）
- [x] 3.2 实测设计书 §3.4 建议的默认组合是否与常见软件冲突，冲突频繁则更换默认值
  - 实施注记：本机真实注册 Alt+Shift+A 成功（无占用），保留默认值 `hotkey.capture="Alt+Shift+A"`
- [x] 3.3 验证程序不在前台、主窗口隐藏时快捷键均生效
  - 实施注记：真实桌面端到端（前台为终端，程序窗口隐藏）SendInput 合成按键 → triggered 恰好一次；机制层为全局注册，与焦点无关
- [x] 3.4 **验证不接收其他按键**：在其他程序中输入文本，确认本程序未收到任何按键，日志中无按键内容
  - 实施注记：真实合成 Shift+H 等未注册组合 → 零触发；注册式热键只收到被注册组合的 WM_HOTKEY，代码路径不接触其他按键；日志只记组合记名与错误码，无按键内容
- [x] 3.5 实现注册冲突的可见提示：用另一程序先占用同一组合，确认本程序给出可读提示、其余功能不受影响、且未夺走对方的热键
  - 实施注记：跨进程实测——进程 A 持有 Alt+Shift+A，进程 B 注册失败且 last_error=「组合键 … 已被其他程序占用」（错误码 1409），A 持续有效（RegisterHotKey 不抢占）；启动时冲突走状态区提示（`_register_hotkey_from_config`→`window.notify`），重绑冲突弹捕获框内反馈
- [x] 3.6 实现保留组合键黑名单，验证通用编辑组合、系统级组合、无修饰键的单键均被拒绝且原设置不变
  - 实施注记：`validate_combo` 四层规则——无修饰键拒绝；Win 参与拒绝；单修饰键拒绝（单 Ctrl=复制粘贴、单 Shift=大写输入、单 Alt=菜单访问/Alt+Tab/Alt+F4）；主键为系统保留键（方向/Tab/Enter/Esc/PrtScn 等）无论修饰键拒绝。保留键补入 VK 表：黑名单语义是「可解析、可读拒绝」而非解析层报错。原设置不变：拒绝发生在 rebind 前且不写配置（单测参数化 12+ 拒绝用例）
- [x] 3.7 新增 `hotkey.capture` 配置项，实现重新绑定：先注销旧组合再注册新组合
  - 实施注记：默认 "Alt+Shift+A"；UI 入口=主窗口工具栏「快捷键」→ HotkeyCaptureDialog（按键捕获，黑名单即时反馈）→ `apply_hotkey_combo`→`GlobalHotkey.rebind`（内部先注销旧再注册新）
- [x] 3.8 验证重绑失败时原组合仍然生效且配置未被更新
  - 实施注记：`test_重绑失败恢复原组合`（新组合被第三方占用 → 原组合恢复注册）+ `test_重绑失败配置保持不变`（配置 MUST NOT 更新）
- [x] 3.9 验证重启后沿用配置的组合；启动时组合被占用则提示且不自行改用其他组合
  - 实施注记：`_register_hotkey_from_config` 读配置注册；占用时 `window.notify("全局快捷键未生效：…")` 状态区提示且保持配置不动（不换组合）；跨进程实测占用/释放两态均符合
- [x] 3.10 验证退出后立即重启能再次成功注册
  - 实施注记：跨进程实测——A 进程退出后立即注册成功（进程终止时 Windows 回收热键注册；显式 unregister 为双保险）；单测注销后同进程再注册亦通过

## 4. 开机启动

- [x] 4.1 实现登录启动项的写入与移除，确认写入当前用户位置、不请求提权
  - 实施注记：`platform/autostart.py`——winreg 直写 `HKCU\...\CurrentVersion\Run`（当前用户，无提权）；enable/disable/heal 单测（独立值名不污染真实键）+ 真实 exe 实测写入成功
- [x] 4.2 新增 `system.auto_start` 与 `ui.start_minimized` 配置项
  - 实施注记：defaults.py 与 config/default.json 同步；托盘勾选状态以注册表真值为准（`_apply_auto_start` 每次回读刷新），配置项同步用户意图
- [ ] 4.3 **真实登录验证**：开启开机启动后注销并重新登录，确认程序自启且驻留托盘不弹窗
  - 实施注记：机制侧已验证（注册表值含 `--tray` + 显示决策矩阵 + 自启抑制）；**注销/重新登录需打断当前用户会话，无法在本会话自动化执行**——留待用户手动验收：勾选托盘「开机启动」→ 注销重登。其余链路（--tray 驻留托盘、托盘不可用显窗、已有实例静默退出）均有单测/真实 exe 证据
- [x] 4.4 实现路径自愈：开启后将整个程序目录移动到新位置再启动，确认登录启动项被更新为新路径
  - 实施注记：真实 exe 实测——预写旧位置值（`C:\Moved Away\...`）→ 从 `...\Temp\OCR Tool E2E\`（含空格新位置）启动 → 注册表值自动重写为新路径引号命令
- [x] 4.5 验证路径未变化时不重复写入
  - 实施注记：`test_路径未变化不重复写入`（值原样保留）；等价写法（大小写/斜杠风格）亦不误判（`_command_paths` 规范化比较）
- [x] 4.6 验证程序位于含空格路径时登录自启仍成功
  - 实施注记：真实 exe 自含空格目录启动成功（22s 就绪含首启杀毒扫描）；注册表值路径引号包裹正确；`test_含空格路径的命令解析往返`
- [ ] 4.7 验证关闭开机启动后重新登录不再自启
  - 实施注记：移除机制单测已覆盖（`test_关闭移除登录启动项`：DeleteValue 后值不存在）；**重新登录部分与 4.3 同因留待用户手动验收**
- [x] 4.8 验证写入或移除失败时开关状态回退到实际生效的状态，不显示为已开启而实际未生效
  - 实施注记：`_apply_auto_start` 捕获 AutoStartError → 状态区提示 + `tray.set_auto_start_checked(注册表真值)`（blockSignals 不回灌）+ 配置同步真值；单测 `test_切换失败时开关回退到实际生效状态`
- [x] 4.9 验证自启时已有实例在运行的情形按重复启动处理，且不显示已有实例的主窗口
  - 实施注记：guard 增 `activate_existing` 参数（--tray 时 False）；真实 exe 实测：A 驻留中 B 以 --tray 启动 → 2s 内退出，A 日志记录「自启拉起时检测到已有实例…不激活其窗口」；单测 `test_自启拉起时不激活已有实例`
- [x] 4.10 验证托盘不可用时的登录自启会显示主窗口，不产生隐形进程
  - 实施注记：`should_show_main_window` 决策矩阵——隐藏意图（--tray/start_minimized）且托盘不可用 → True（单测参数化）；托盘降级路径见 2.9

## 5. 整合与收尾

- [x] 5.1 若 `screen-region-ocr` 已完成，将快捷键绑定动作改为截图识别并验证端到端流程
  - 实施注记：`_on_global_hotkey` → `bring_to_front` + `start_region_capture`（唤起截图识别，结果不静默丢弃）。真实 exe 端到端验证：合成 Alt+Shift+A → 全屏覆盖层出现（1920×1080 Qt Tool 置顶窗口）→ 合成 Esc → 覆盖层销毁、主窗口回前台，E2E PASS
- [ ] 5.2 常驻 8 小时以上，记录基线内存占用并确认无持续增长；确认未识别过时进程内存不含模型权重
  - 实施注记：基线已记录——真实 exe --tray 驻留（含热键注册与托盘）约 **98 MB**（100,112K），120 秒间隔复测无增长（100,096K）；98 MB 远低于模型加载后水位（Small 权重 + onnxruntime 运行时为数百 MB 级）→ 未识别过不含模型权重成立。**8 小时长跑观察无法在本会话内完成，留待用户执行**（建议驻留一日后用任务管理器复查内存仍在 ~100MB 量级）
- [x] 5.3 确认日志中不含任何按键内容与识别文本，未破坏 `app-logging` 的隐私约定
  - 实施注记：本变更新增日志逐点审查——只记组合记名（程序注册的组合标识，非用户按键内容）、端点哈希、路径、错误码；热键触发路径无日志（不记触发事件）。设计书 §43 同步补「日志禁止记录任何按键内容」。既有 `test_logging_privacy` 回归通过
- [x] 5.4 设计书新增单实例章节，说明陈旧端点清理的必要性
  - 实施注记：§36.1「单实例（三项功能的共同前提）」——端点派生、陈旧清理的「一次崩溃变永久故障」机理、互斥量仲裁（并发竞态）
- [x] 5.5 设计书 §42 / §43 补充登录启动项写入注册表的说明，含位置与手动移除方式，并说明删除程序目录后该项无法自动清理
  - 实施注记：§43.1——位置（HKCU Run）、值名与格式、三种手动移除方式、删除目录后遗留项的限制、路径自愈；§43 日志禁止清单补「任何按键内容」
- [x] 5.6 在设计书 §36 勾选「全局快捷键」「开机启动」「系统托盘」
  - 实施注记：三项已勾选（UI 设置窗口仍未实现，保持未勾）
- [x] 5.7 `openspec validate --strict background-residency` 通过
  - 实施注记：`Change 'background-residency' is valid`；全量测试 406 passed（含本变更新增 5 个测试文件与既有回归）
