# 任务：模型切换

## 0. 前置确认

- [ ] 0.1 确认 `mvp-image-ocr` 已完成，单模型识别链路可正常工作
- [ ] 0.2 确认 PP-OCRv6 Tiny 的检测与识别模型文件可获取，并取得其官方 SHA256

## 1. 模型资产扩充与枚举

- [ ] 1.1 将 Tiny 模型登记进 `packaging/models.lock.json`，扩充获取脚本
- [ ] 1.2 获取 Tiny 模型并确认哈希校验通过，模型目录符合既有目录契约
- [ ] 1.3 实现可用模型枚举，返回标识与展示名称，顺序稳定
- [ ] 1.4 验证：运行期间放入一个新模型目录后再次打开列表，新模型出现且无需重启
- [ ] 1.5 验证：描述文件缺失或模型文件不存在的目录被跳过并记录日志
- [ ] 1.6 验证：仅有一个模型时列表正常返回，不报错

## 2. 切换语义

- [ ] 2.1 实现「先加载新引擎、成功后释放旧引擎」的切换流程
- [ ] 2.2 验证失败回滚：故意损坏目标模型文件后触发切换，确认仍使用原模型、识别功能可用、提示可读、日志含技术细节
- [ ] 2.3 实现与在途识别的互斥：在识别执行中触发切换，验证该识别用旧模型正常返回结果，切换随后执行；重复至少 10 次确认无时序竞争
- [ ] 2.4 实现切换期间拒绝新识别，验证不产生请求堆积
- [ ] 2.5 验证切换进行中关闭程序可正常退出
- [ ] 2.6 验证切换到当前已在使用的模型时不重新加载
- [ ] 2.7 验证未加载任何模型时切换仅记录选择、不触发加载

## 3. 持久化与内存

- [ ] 3.1 实现切换成功后写入用户配置，验证重启后沿用
- [ ] 3.2 验证切换失败时用户配置保持不变
- [ ] 3.3 验证配置中记录的模型被删除后，按既有回退顺序选择并记录错误日志
- [ ] 3.4 反复切换 20 次，观察进程内存在每次切换后回落到单模型水平，不持续增长
- [ ] 3.5 验证日志记录了每次切换的来源模型、目标模型与加载耗时

## 4. 界面

- [ ] 4.1 实现模型选择入口，标示当前使用的模型；用统一样本实测 Tiny 与 Small 的耗时与识别效果差异并记录
- [ ] 4.2 实现状态区的结果产出模型标注，记录加入 Tiny 后发布产物的体积增量
- [ ] 4.3 验证切换后旧结果保留且标注仍为切换前的模型
- [ ] 4.4 验证用新模型重新识别后标注随之更新
- [ ] 4.5 验证识别或切换进行中，模型选择入口不可用

## 5. 收尾

- [ ] 5.1 回写设计书 §12.1：调用链顺序改为先加载后释放，补充失败回滚说明
- [ ] 5.2 在设计书 §36 勾选「Tiny / Small 模型切换」
- [ ] 5.3 `openspec validate --strict model-switching` 通过
