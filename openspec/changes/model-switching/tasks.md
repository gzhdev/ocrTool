# 任务：模型切换

## 0. 前置确认

- [x] 0.1 确认 `mvp-image-ocr` 已完成，单模型识别链路可正常工作
  （上一变更收尾时全量 278 passed；本次抽样复跑 model-assets/service/controller 39 passed）
- [x] 0.2 确认 PP-OCRv6 Tiny 的检测与识别模型文件可获取，并取得其官方 SHA256
  （取自本地 rapidocr 3.9.2 包 default_models.yaml 的 onnxruntime 段官方登记值：
  det `f42c0fbd…`、rec `e16e242d…`，与既有 Small 条目同源同构）

## 1. 模型资产扩充与枚举

- [x] 1.1 将 Tiny 模型登记进 `packaging/models.lock.json`，扩充获取脚本
  （fetch_models.ps1 按 lock 驱动遍历，登记条目即自动扩充，脚本本身无需改动）
- [x] 1.2 获取 Tiny 模型并确认哈希校验通过，模型目录符合既有目录契约
  （脚本输出「落地并校验通过」×2；补 model.json 后 scan_models 两模型零错误）
- [x] 1.3 实现可用模型枚举，返回标识与展示名称，顺序稳定
  （model_manager.available_models()：每次重扫描、目录名排序、不做哈希校验）
- [x] 1.4 验证：运行期间放入一个新模型目录后再次打开列表，新模型出现且无需重启
  （test_运行期放入新模型后再枚举即可见）
- [x] 1.5 验证：描述文件缺失或模型文件不存在的目录被跳过并记录日志
  （test_不完整目录被跳过并记录日志，日志含跳过原因）
- [x] 1.6 验证：仅有一个模型时列表正常返回，不报错
  （test_仅一个模型时正常返回不报错；另加枚举不做哈希校验的行为差异用例）
  （test_model_assets.py 13 → 19 passed）

## 2. 切换语义

- [x] 2.1 实现「先加载新引擎、成功后释放旧引擎」的切换流程
  （OCRService.switch_model：同 id 无操作 → 未加载仅记录 → 构造新引擎成功后
  替换并 del 旧引擎；worker.py 新增 ModelSwitchWorker 复用容量 1 线程池）
- [x] 2.2 验证失败回滚：故意损坏目标模型文件后触发切换，确认仍使用原模型、识别功能可用、提示可读、日志含技术细节
  （双实证：单测 test_新模型加载失败_旧引擎原封可用 + 集成 test_损坏目标模型文件后切换
  （det.onnx 截断为 64 字节的真实文件损坏）；message 不含技术细节、detail 与 exc_info 含）
- [x] 2.3 实现与在途识别的互斥：在识别执行中触发切换，验证该识别用旧模型正常返回结果，切换随后执行；重复至少 10 次确认无时序竞争
  （识别在途时控制器暂存切换请求、识别结束后接续提交（design D2 的等待
  语义在控制器实现），容量 1 线程池为兜底防线；
  test_识别在途时切换排队_识别先用旧模型完成 + 重复十次用例，每轮断言
  结果到达 1 次、switch_calls 恰为目标、无错配）
- [x] 2.4 实现切换期间拒绝新识别，验证不产生请求堆积
  （切换全程 busy=True，start_recognition 返回 False；连续 5 次被拒请求
  在切换结束后 recognize_calls==0，恢复后恰执行 1 次）
- [x] 2.5 验证切换进行中关闭程序可正常退出
  （集成 test_切换进行中关闭程序可正常退出：真实引擎加载中 window.close()
  立即返回，事件循环持续运转至后台切换完成，无挂起无崩溃）
- [x] 2.6 验证切换到当前已在使用的模型时不重新加载
  （service：model_id 相同直接 return；test_切换到当前模型不重新加载 +
  UI 层 test_选择当前模型是无操作）
- [x] 2.7 验证未加载任何模型时切换仅记录选择、不触发加载
  （service：_engine is None 时只换 _model 与参数；test_未加载任何模型时
  切换仅记录选择——首次识别加载的正是新记录的模型）

## 3. 持久化与内存

- [x] 3.1 实现切换成功后写入用户配置，验证重启后沿用
  （MainWindow._on_model_switched → config.set("ocr.model")+save，复用
  model-assets 既有 ocr.model 配置项与启动解析链；test_切换成功写入用户配置）
- [x] 3.2 验证切换失败时用户配置保持不变
  （test_切换失败用户配置保持不变：snapshots 为空、值保持 id-a）
- [x] 3.3 验证配置中记录的模型被删除后，按既有回退顺序选择并记录错误日志
  （resolve_model 为唯一决策点，单测（model-assets）+ 集成
  test_配置记录的模型被删除后回退到推荐模型并记日志 双实证）
- [x] 3.4 反复切换 20 次，观察进程内存在每次切换后回落到单模型水平，不持续增长
  （集成 test_反复切换20次_旧引擎全部释放内存回落：真实 onnxruntime 引擎
  small↔tiny 交替 20 轮，weakref 断言任意时刻至多 1 个引擎存活；进程 RSS 受
  onnxruntime 内存池归还策略影响只作参考不作硬断言，引擎对象零驻留是必要条件）
- [x] 3.5 验证日志记录了每次切换的来源模型、目标模型与加载耗时
  （service 日志「开始切换模型：id-a → id-b」+「模型切换完成：… 新引擎
  加载耗时=xms」；test_切换日志记录来源目标与耗时）

## 4. 界面

- [x] 4.1 实现模型选择入口，标示当前使用的模型；用统一样本实测 Tiny 与 Small 的耗时与识别效果差异并记录
  （工具栏「模型」QToolButton + 菜单每次呈现前重扫目录、当前模型打勾；
  实测（2026-08-24，开发机，3 次取最优）——加载：Small 882ms / Tiny 195ms
  （约 4.5×）；识别：english 523→248、chinese 264→115、mixed 493→245、
  small_text 403→220（约 2×）；四个合成样本两模型行数与文本完全一致，
  精度差异在合成样本上不可分，待真实场景观察（design Open Question 留答））
- [x] 4.2 实现状态区的结果产出模型标注，记录加入 Tiny 后发布产物的体积增量
  （OcrResult 新增 model_name 字段随结果携带；状态区模型段语义=当前展示
  结果的产出模型；体积增量：Tiny det 1.74MB + rec 4.28MB = +6.0MB，
  现有 Small 29.7MB，发布产物约 +6MB（约 +20%））
- [x] 4.3 验证切换后旧结果保留且标注仍为切换前的模型
  （test_切换后旧结果保留且标注仍为旧模型：文本、框、耗时行数、模型标注
  全部保持；切换期间不清运行信息）
- [x] 4.4 验证用新模型重新识别后标注随之更新
  （test_切换后用新模型重新识别_标注随之更新：模型A→切换B→标注仍A→
  重新识别→标注B）
- [x] 4.5 验证识别或切换进行中，模型选择入口不可用
  （_update_actions 按 busy 统一禁用 _model_button；识别/切换两场景均测，
  _request_model_switch 内再查为双保险）

## 5. 收尾

- [x] 5.1 回写设计书 §12.1：调用链顺序改为先加载后释放，补充失败回滚说明
- [x] 5.2 在设计书 §36 勾选「Tiny / Small 模型切换」
- [x] 5.3 `openspec validate --strict model-switching` 通过
