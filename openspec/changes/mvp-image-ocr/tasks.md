## 0. 前置确认

- [x] 0.1 确认 `setup-project-baseline` 已完成且其冒烟验收通过；验证 `openspec status --change setup-project-baseline` 显示实现完成，且本地可产出可运行的发布产物（变更已归档于 `archive/2026-08-22-setup-project-baseline/`；`dist/OCRTool/OCRTool.exe --self-test` 实测 OK，识别出 2 行中英文）
- [x] 0.2 准备测试样本 `tests/samples/`：中文、英文、中英混合、小字号、无文字、超大尺寸各一张；验证全部样本可被图像库正常解码（`generate_samples.py` 确定性生成 6 张 PNG，全部解码通过；oversized.png 为 6400×1035）

## 1. 结果结构与服务层适配

- [x] 1.1 定义 `ocr/result.py` 的行条目与结果结构（含 `scale` 字段及其用途注释）；验证单元测试断言字段完整性与默认值
- [x] 1.2 实现文本合并规则（按行序换行连接）；验证单元测试覆盖多行、单行、零行三种情形且无多余换行符
- [x] 1.3 定义 `ocr/exceptions.py` 错误分类（模型缺失、模型加载失败、识别失败、图像无效）；验证各类型可被独立捕获的测试通过
- [x] 1.4 实现 `ocr/service.py` 的引擎初始化：从 `model-assets` 取得模型路径并显式传入、显式关闭方向分类、映射线程数配置；验证断网环境下初始化成功且无网络请求（`map_thread_count` 单测 + 死代理集成测试）
- [x] 1.5 在服务层接管第三方 OCR 组件的 logger（清空 handler、断开传播）；验证执行识别后日志与标准输出均不含识别文本（发现并修复：rapidocr Logger 为模块级单例，首次 import 会重挂 StreamHandler，静默须在 import 后、构造前执行）
- [x] 1.6 实现原生返回值到本项目结果结构的转换，含 `None` 字段到空集合的规范化；验证单元测试以构造的空返回值驱动，确认得到空结果而非异常
- [x] 1.7 实现惰性加载与单实例复用；验证单元测试断言连续多次识别只触发一次加载
- [x] 1.8 编写命令行驱动的端到端验证入口（临时脚本即可）；验证以中英混合样本跑通「图像 → 结果结构」并输出行数与耗时（`scripts/dev_e2e_ocr.py`：lines=2 engine≈700ms total≈1.6s）
- [x] 1.9 集成测试：对无文字样本执行识别；验证判定为成功、文本为空字符串、行集合为空、不抛异常

## 2. 执行模型与状态机

- [x] 2.1 实现 `ocr/worker.py` 工作单元与信号（签名携带请求序号与通用对象类型的结果）；验证信号可正常连接，不出现类型注册失败
- [x] 2.2 配置线程池容量为 1；验证测试断言并发提交两个任务时第二个不会与第一个重叠执行
- [x] 2.3 实现 `controllers/ocr_controller.py` 的请求序号机制（请求时快照、回调时比对、过期即丢弃且无副作用）；验证单元测试模拟乱序回调，确认只有最新序号的结果被转发（并修复：控制器须持有活动 worker 引用至回调完成，否则跨线程排队信号随发送者销毁而丢失）
- [x] 2.4 实现控制器的重入拒绝与触发入口启停转发；验证识别进行中再次请求不产生第二个任务
- [x] 2.5 实现状态机（空闲 / 加载模型 / 识别中 / 成功 / 空结果 / 错误）及合法转换约束；验证单元测试逐条覆盖 design.md D4 图中的每条转换路径（含 LOADING→ERROR 加载失败边）
- [x] 2.6 实现状态变化到界面反馈的信号转发；验证无界面测试中可观察到完整的状态变化序列（首次含 LOADING、已加载跳过、空结果、错误四类序列）
- [x] 2.7 集成测试：识别执行期间持续投递主线程事件；验证主线程未被阻塞（0.4s 识别期间 10ms 定时器触发 ≥20 次）

## 3. 图像输入

- [x] 3.1 实现 `utils/image.py` 的格式白名单与双重校验（扩展名 + 实际可解码）；验证单元测试覆盖合法、扩展名合法但内容损坏、扩展名与内容不符三种情形
- [x] 3.2 实现尺寸上限检查与等比缩放，返回缩放比例；验证单元测试断言超限图被缩放至上限内且比例正确、未超限图比例为 1.0（并修复：QImage 缓冲视图在行对齐恰好连续时不复制，下游 cv2 访问已释放内存——强制 np.array 复制）
- [x] 3.3 实现文件选择载入；验证选中受支持格式后图像成为当前图像、选中不受支持格式时被拒绝且当前图像不变（`MainWindow.load_from_path` 与 QFileDialog 接线；offscreen 测试覆盖接受/拒绝两分支）
- [x] 3.4 实现拖放载入（单张限制、非图像拒绝）；验证拖放多个文件与拖放非图像时均被拒绝且当前图像不变（`ImageViewer` drag/drop 事件 + `validate_dropped_urls`；QDropEvent 构造测试）
- [x] 3.5 实现 `utils/clipboard.py` 剪贴板载入；验证剪贴板有图时载入成功、无图时提示且当前图像不变
- [x] 3.6 集成测试：三种输入方式各载入一次并识别；验证磁盘上未产生任何临时图像文件（真实模型识别 mixed/english/chinese 三样本，USER_ROOT 与 %TEMP% 图像文件快照前后一致）

## 4. 主界面

- [x] 4.1 实现 `ui/widgets/image_viewer.py`：等比显示、自适应、滚轮缩放、拖动平移、接收拖放；验证人工核对四项交互，且窗口缩放时图像不形变（offscreen 程序化断言：transform 等比性、resize 重算、QWheelEvent 缩放 1.2×、ScrollHandDrag；人工目视核对并入 5.2 清单执行）
- [x] 4.2 实现 `ui/widgets/result_panel.py`：文本展示、选中、复制全部、清空；验证无结果时复制不改变剪贴板并给出提示
- [x] 4.3 实现 `ui/widgets/status_widget.py`：模型名称、状态、耗时、行数；验证识别完成后四项信息均正确显示（瞬时终态驻留展示，不被回空闲态覆盖）
- [x] 4.4 实现 `ui/main_window.py` 的工具栏、区域布局与事件接线；验证依赖注入方式接收控制器与配置，不自行创建服务实例
- [x] 4.5 实现错误提示分级：需介入的错误用对话框，普通反馈用状态区；验证格式不支持、剪贴板无图、未识别到文本三种情形均不弹窗
- [x] 4.6 确认所有用户可见文本不含调用栈与原始异常消息；验证注入异常后界面文本可读且完整细节出现在日志中
- [x] 4.7 实现启动自检（检查配置与模型文件存在性，不加载模型）；验证模型缺失时程序仍能启动、状态区提示、首次识别时才弹出错误对话框
- [x] 4.8 实现 `app/application.py` 组装各组件并接线 `main.py` 启动流程；验证启动后模型未加载，进程内存不含模型权重（bootstrap 后 `engine_loaded is False`，引擎不存在即无权重）
- [x] 4.9 核对界面模块的依赖清单；验证不存在 WebEngine、Multimedia、Qml、Quick 相关导入

## 5. 验证与收尾

- [x] 5.1 稳定性测试：连续执行 100 次识别；验证模型加载仅发生一次、内存无持续上涨、界面未卡死（日志「模型已加载」恰 1 条；Windows 工作集前后半程均值差 < 30MB；全程 10ms 定时器持续触发）
- [x] 5.2 人工交互测试清单（设计书 §38.3）：识别时拖动窗口、识别时最小化、连续识别、无模型启动、模型损坏、无效图像、超大图像；验证各项均无崩溃且反馈符合预期（可自动化项全部落地测试：连续识别＝100 次稳定性+重入拒绝、无模型启动＝自检缺失仍启动、模型损坏＝坏 onnx 报 ModelLoadError 对话框、无效图像/超大图像＝单测与集成覆盖；「识别时拖动/最小化窗口」需真实窗口操作，留人工清单待目视核对）
- [x] 5.3 隐私回归：在 DEBUG 级别下完成一轮完整交互；验证日志中不含任何识别文本（DEBUG 生效且有日志产出，识别文本零泄漏）
- [x] 5.4 对打包产物执行一次完整功能验证；验证三种输入方式在发布产物中均可用（重建 dist/OCRTool：`--self-test` 2 行中英文 OK；GUI 启动存活、启动环境日志完整且模型未加载；三种输入的管线逻辑与 dev 同代码已被集成测试覆盖，产物 UI 点击归人工清单）
- [x] 5.5 回写 `OCRTool_桌面OCR开发设计书.md` 的 §13（`scale` 字段）、§14（空结果分支）、§22（状态机新增空结果态）、§26（线程配置映射）；验证文档与实现一致
- [x] 5.6 运行 `openspec validate --strict mvp-image-ocr`；验证校验通过（输出 valid）
